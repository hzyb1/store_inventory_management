<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.InStockDetailMapper">
<select id="selectById" parameterType="model.InStockDetail">
     select * from instockdetail where id=#{id}
  </select>
  <select id="selectAll" resultType="model.InStockDetail">
      select * from instockdetail 
  </select>
  <update id="upateInStockDetail" parameterType="model.InStockDetail">
      update instockdetail set instockid=#{instockid},productid=#{productid},amount=#{amount}
		where id = #{id}
  </update>
  <insert id="insertInStockDetail" useGeneratedKeys="true" keyProperty="id">
      insert into instockdetail(instockid,productid,amount)
      values(#{instockid},#{productid},#{amount})
  </insert>
  <delete id="deleteById" parameterType="id">
      delete form instockdetail where id=#{id}
  </delete>
</mapper>
